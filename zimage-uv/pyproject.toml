# Auto-generated by `aisongtool setup`. Isolated uv env for zimage-uv.
[project]
name = "zimage-uv"
version = "0.0.0"
requires-python = ">=3.10,<3.13"
dependencies = [
    "torch==2.8.0",
    "diffusers @ git+https://github.com/huggingface/diffusers.git",
    "transformers",
    "accelerate",
    "safetensors",
    "pillow",
    "gradio",
    "bitsandbytes"
]
