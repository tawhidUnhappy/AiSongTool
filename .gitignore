# Runtime
jobs/
output/
desktop-settings.json

# Python environments
.venv/
venv/
demucs-uv/.venv/
whisperx-uv/.venv/
zimage-uv/.venv/
gemma-uv/.venv/
syrex-uv/.venv/

# Portable dev-only tool runtimes (e.g. Node.js, downloaded as a zip — not a
# system install, not authored here)
.tools/

# Electron app (desktop/) — node_modules + build output, not the app's source
desktop/node_modules/
desktop/out/
desktop/dist/
desktop/*.tsbuildinfo

# ACE-Step (acestep.cpp) — downloaded prebuilt binaries + GGUF models, not
# authored here
acestep-cpp/

# Python cache
__pycache__/
*.pyc
*.pyo

# Build artifacts
*.egg-info/
dist/
build/

# Per-user data dir created by frozen installs (see aisongtool/paths.py)
.aisongtool/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
.claude/
